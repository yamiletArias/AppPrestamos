<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main"
    android:orientation="vertical"
    android:padding="16dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- RecyclerView para mostrar cada pago realizado -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerPagosRealizados"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:scrollbars="vertical" />

    <!-- TextView fijo al final para mostrar el total de pagos realizados -->
    <TextView
        android:id="@+id/tvTotalPagos"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Total de pagos: 0"
        android:textSize="16sp"
        android:gravity="end"
        android:layout_marginTop="8dp" />
</LinearLayout>
